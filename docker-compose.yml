version: "3.9"

services:
  simplebank:
    image: 127348474924.dkr.ecr.us-east-2.amazonaws.com/flaskapp-simple-bank:v1
    container_name: simplebank
    ports:
      - "5000:5000"
    environment:
      - "FLASK_ENV=production"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s